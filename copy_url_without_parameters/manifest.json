{
    "manifest_version": 2,
    "name": "Copy URL Without Parameters",
    "description": "Adds a context menu option and hotkey shortcut to copy a URL while removing all parameters",
    "version": "1.0",
    "homepage_url": "https://github.com/voidsmith-releases/firefox_addons/tree/master/copy_url_without_parameters",

    "background": {
        "scripts": [
            "background.js"
        ]
    },

    "permissions": [
        "activeTab",
        "contextMenus",
        "clipboardWrite"
    ],    

  "commands": {
    "toggle-feature": {
      "suggested_key": { "default": "Ctrl+Alt+C" },
      "description": "Copies URL of current tab while removing all parameters"
    }
  }
}
